<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!--
      Only the statements folder is exposed, and only from cache. Nothing else
      in the app's storage is reachable by another app through this provider.
    -->
    <cache-path
        name="statements"
        path="statements/" />
</paths>
