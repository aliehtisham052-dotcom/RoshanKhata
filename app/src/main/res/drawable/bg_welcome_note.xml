<?xml version="1.0" encoding="utf-8"?>
<!-- The welcome note as a quiet card: a faint white wash with a soft gold edge on the green. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/welcome_note_fill" />
    <stroke android:width="1dp" android:color="@color/welcome_note_stroke" />
    <corners android:radius="14dp" />
</shape>
