<?xml version="1.0" encoding="utf-8"?>
<!-- Small square behind the arrow on the Home "I have to get" card: the same tint as the summary box it replaced. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/summary_get_bg" />
    <corners android:radius="7dp" />
</shape>
