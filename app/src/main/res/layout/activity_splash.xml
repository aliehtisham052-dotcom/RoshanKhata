<?xml version="1.0" encoding="utf-8"?>
<!--
  A minimal opening screen: just the "Powered by Innovation-313" line on the app
  background, briefly, before the ledger opens. No logo image (the old one had a
  black box the owner disliked) — only the wordmark line they asked for.
-->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/brand_green">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:text="@string/powered_by"
        android:textColor="#FFFFFF"
        android:textSize="20sp"
        android:textStyle="bold"
        android:letterSpacing="0.02" />

</FrameLayout>
